package org.junaed.vending_machine.ui.utils

/**
 * Android implementation of openUrlInBrowser.
 * This could be updated to use Android's Intent system to actually open browsers,
 * but for now it's just a no-op as per the current requirements.
 */
actual fun openUrlInBrowser(url: String) {
    // No-op on Android platform
}
